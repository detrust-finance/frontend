export interface LanguageContextProps {
  language: unknown
  changeLanguage?: any
}

export interface ResponsiveContextProviderProps {
  isDesktop: boolean
  isLaptop: boolean
  isTablet: boolean
  isMobile: boolean
  isPortrait: boolean
}
